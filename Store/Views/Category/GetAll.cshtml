@using Application.ViewModel.Category
@model List<CategoryViewModel>


@foreach (var item in Model)
{
    <div class="text-center">
          <table class="table">
        <thead>
            <tr>

                <th>
                    title
                </th>
                <th>
                    عملیات
                </th>
            </tr>
        </thead>

        <tbody>
            <tr>

                <td>
                    @item.Title
                </td>
                <td>
                    <a asp-controller="Category" asp-action="Update" asp-route-id="@item.Id">Update</a>
                    <a>Delete</a>
                </td>
            </tr>
        </tbody>

    </table>
    </div>
  
}
